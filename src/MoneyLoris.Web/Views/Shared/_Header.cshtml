@using System.Security.Claims;

@{
    bool? esconderHeader = (bool?)ViewData["EsconderHeader"];
}

@if (!esconderHeader.HasValue || esconderHeader.Value == false)
{
    <header>

        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand" href="/">
                    <img src="img/moneyloris-logo-text.svg" alt="MONEY LORIS">
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                @if (true @*User.Identity!.IsAuthenticated*@)
                {
                    <div class="collapse navbar-collapse" id="navbarSupportedContent">
                        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                            
                            @if (!User.IsInRole("Administrador"))
                            {
                                <li class="nav-item">
                                    <a class="nav-link" href="/Lancamento">Lançamentos</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="#">Contas e Cartões</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="#">Categorias</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="#">Relatórios</a>
                                </li>
                            }
                            else
                            {
                                <li class="nav-item">
                                    <a class="nav-link" href="/Admin/Conta">Contas de Usuário</a>
                                </li>
                            }

                            <li class="nav-item d-lg-none">
                                <a class="nav-link" href="#" id="pwaAppInstaller">Instale o Money Loris no seu celular</a>
                            </li>

                        </ul>

                        <div class="d-inline-flex align-items-center">
                            <a class="nav-link me-3" href="#">
                                <i class="fa-solid fa-user fa-lg sys-fg-dark-3"></i>
                                @*<span class="ms-1">@User.UsuarioNome().PrimeiroNome()</span>*@
                                <span class="ms-1">Ronaldinho</span>
                            </a>
                            <a class="btn btn-secondary sys-fg-dark-2 fw-bold" href="/Logout">Sair</a>
                        </div>
                    </div>
                }

            </div>
        </nav>

    </header>
}